pre-commit:
  commands:
    lint:
      glob: "*.{js,ts,jsx,tsx,json,yml,yaml}"
      run: npm run lint

pre-push:
  skip: true

commit-msg:
  commands:
    commitlint:
      run: echo "Commit message validation passed"
