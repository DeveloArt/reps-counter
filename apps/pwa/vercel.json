{
  "buildCommand": "cd ../.. && pnpm install && cd apps/pwa && pnpm run build",
  "outputDirectory": "dist",
  "installCommand": "pnpm install",
  "framework": "vite",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD ./apps/pwa"
}
